name: machine_data
version: '1.0.0'

profile: machine_data

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]
macro-paths: ["macros"]
analysis-paths: ["analyses"]

models:
  machine_data:
    staging:
      +materialized: view

    marts:
      +materialized: table